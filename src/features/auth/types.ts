export type Login= {
    username : string,
    password : string,
}
export type User= {
    id : string,
    lastName: string,
    firstName: string,
    token : string,
    username : string,
    refreshToken: string,
}